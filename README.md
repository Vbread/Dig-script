# Dig-script

a unity script for digging. works based off polys. 

digging is done with colision. 

WARNING - if indending to use the dig, refactor if using 'drill' type dig. continuous digging can cause lag. 
